# Architecture Overview

Architecture diagrams, expanded data models, and decision records (ADRs) that back [`03-TDD.md`](../03-TDD.md).

**Boundary (r6, TDD §2):** the TDD keeps the decisions and their summary (it remains one third of the cohesive PRD/EDD/TDD set); this directory holds the supporting detail the TDD links into — full diagrams, expanded data models, and ADR history. Created on first use.

## Manifest

*(Empty — populated on first use. ADRs land here as `NNNN-<slug>.md`; diagrams and data-model detail as needed.)*
