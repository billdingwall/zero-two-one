# r7 Update Plan: 04-BACKLOG.md

**Status:** Draft — **Pending approval** (no edit until approved, RLP step 2)
**Date:** 2026-07-15
**Round:** r7
**Audit items addressed:** all groups A–H (tracking rows), G (re-scoped items)
**Target doc:** [../04-BACKLOG.md](../04-BACKLOG.md)
**References:** [_notes/full-repo-audit.md](../_notes/full-repo-audit.md) · [r7-review.md](r7-review.md)

## Intent

Track every audit item in the backlog table: the r7 in-round fixes as `Done` rows (once applied) under the current release, and the re-scoped engine work under its mvp release. Mirrors [r7-update-roadmap.md](r7-update-roadmap.md) exactly.

## Proposed Edits

### 1. New rows — r7 in-round fixes (groups A–F, H)

Added with `Release` = mvp-2 follow-on (current Planning work), `Status` = Done at round close:

| Item (summary) | Ownership |
|---|---|
| A1–A3: init interim guards (arg gating + `--help`/`--version`, create-if-missing, hook backup) | Eng |
| A4: init installs `.claude/commands/` merge-safe (TDD §4 parity) | Eng |
| B1: remove dangling `main` (both manifests) | Eng |
| B2: delete `package/.ai/context/001-dummy.*` + sync rule (empty scaffold only) | Eng |
| B3: LICENSE (ISC) at root + in package `files` | Eng |
| B4: stop shipping `prototype/` (drop from `files` + init copy; generated by `021-prototype`) | Eng |
| B5: fill `author`; pin Spec Kit install guidance | PM |
| C1: fix `run-qa.sh` 3-phase mapping (r6 escaped defect) | Eng |
| C2: `workflow-status.js --json`; `run-qa.sh` consumes it | Eng |
| C3: `"private": true` on root manifest | Eng |
| C4: fix stale `.gitignore` comment | Eng |
| C5: fold/tombstone `.021-updates/` into `_notes/` | PM |
| D1: GitHub Actions workflow (test · init smoke · link check · sync-drift) | Eng |
| D2: `sync:package --check` drift mode (+ `package/.claude` divergence closed) | Eng |
| D3: lint baseline — built-ins-only (`node --check`, `sh -n`) per zero-dep posture *(assumption; confirm)* | Eng |
| E1–E2: old-phase-label sweep repo+package; era note in `_refinement/_INDEX.md` (annotate, don't rewrite history) *(assumption; confirm)*; grep gate in CI | PM |
| F1: README(s) three-stack prompts labeled (Antigravity/Kiro land mvp-4); roadmap clarity line | PM |
| H1: CONTRIBUTING.md (+ optional CODEOWNERS) | PM |

### 2. Re-scoped rows (group G)

- **Move** "Automated tests for `bin/init.js` and `hooks/pre-commit`" from mvp-6 → **mvp-3** (exit-gate item; fixture-repo harness).
- **Amend** mvp-3 Workflow Manager row: "read-only `021-doctor` reporter first" (TDD §13).
- **Add** mvp-3 row: manifest-as-QA-contract (single `lib.js` parser for `run-qa.sh` + pre-commit).
- **Add** mvp-4 row: programmatic `exports` decision (`zero-two-one/speckit`).
- **Amend** mvp-6 publish row: CI-only, tag-triggered, `--provenance`, pre-publish gate.

### 3. Open questions & cycles

- Close: "Is anything published to npm?" → verified 404 (r7).
- Add r7 row to Refinement Cycles; changelog entry.

## Cascade

- Mirrors [r7-update-roadmap.md](r7-update-roadmap.md); execution of the in-round items in [r7-update-workflows.md](r7-update-workflows.md).
