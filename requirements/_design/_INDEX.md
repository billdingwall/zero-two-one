# Design Assets Overview

Directory that holds design assets and design-reference docs.

## Manifest

- [command-design.md](command-design.md): How every command surface maps together — npm scripts, assistant-rendered commands, agent skills, and underlying scripts (r5).
- [workflow-design.md](workflow-design.md): How each workflow and git hook reads/writes project files, plus the manifest as shared state (r5).
- `tokens/`: Exported design-system token artifacts (created by `/021-design`; see [design-system-selection](../../workflow/specific-workflows/design-system-selection.md)). *(Created on demand.)*
