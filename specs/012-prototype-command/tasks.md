# Tasks: Optional Prototype Command (`021-prototype`)

*Generated from [plan.md](plan.md), [data-model.md](data-model.md), [contracts/prototype-command.md](contracts/prototype-command.md), [research.md](research.md), and [quickstart.md](quickstart.md). Dependency-ordered; tests-first. `[P]` = parallelizable. The thin `scripts/prototype.js` (scaffold a bare themed `prototype/` skeleton, zero-dep) is the core; the dispatcher route and command surface ride on it. Wire-in is emergent — no edits to `run-qa.sh` / `prototype-sync.md` / `workflow-status.js`. Mirrors specs 010/011; cross-stack render is free via the spec-007 `021-*.md` surface-render.*

**Proposed layout:** new `scripts/prototype.js`; new `.claude/commands/021-prototype.md`; `bin/021.js` (+`prototype`→`init` route + usage line); `scripts/_INDEX.md` update; tests in `test/prototype/prototype.test.js` (new) + two expectation-list folds (`surface.test.js`, `dispatch.test.js`).

## Phase 1 — Setup & guardrails
- [x] T001 **Confirm the seams before writing code.** `scripts/init/**` has **no** `prototype` reference → init does not create `prototype/`; `prototype.js` `mkdir -p prototype/` + seeds `_INDEX.md` (research R1). The presence threshold is any file except `_INDEX.md` (research R3). CSS seam = `@import ../requirements/_design/tokens/*.css` when `tools.design !== none` + a tokens `*.css` exists, else inline `:root` from `DESIGN.md` frontmatter (research R2). Wire-in is **emergent** — `run-qa.sh:64` gates on `ls prototype/*.html`, `prototype-sync.md` on `> _INDEX.md`, `workflow-status.js` has no prototype ref → **do not edit those three files** (research R4). `bin/021.js` `resolve()` gets a `prototype`→`init` leaf (mirrors `design`). New command file does **not** re-baseline `claude-golden.json` (research R5). No task changes these facts — this pins them.
- [x] T002 [P] **Test helpers** — a temp-project helper that seeds a git repo + a `.zero-two-one.json` (chosen `tools.design`) + optional `requirements/_design/tokens/*.css` + a `DESIGN.md`; a runner for `node scripts/prototype.js <args>` and `node bin/021.js prototype <args>` capturing stdout/stderr/exit.

## Phase 2 — Tests first (author before their Phase 3 counterparts)
- [x] T003 [P] **Scaffold** (C1): `init` on a bare project (no `prototype/`, or only `_INDEX.md`) creates `prototype/{index.html,styles.css,app.js}` and seeds `_INDEX.md`; `index.html` links `styles.css` and `app.js` (Scenario 1; FR-002). Asserts `prototype/` is created when absent (init does not; research R1).
- [x] T004 [P] **Design-system seam — `@import`** (C1): with `tools.design: material-3` and a `requirements/_design/tokens/tokens.css` present, `styles.css` starts with `@import '../requirements/_design/tokens/…css';` (Scenario 2/3; FR-004).
- [x] T005 [P] **Design-system seam — inline `:root`** (C1): with `design: none` (or no tokens `*.css`), `styles.css` has an inline `:root { --… }` block derived from the `DESIGN.md` frontmatter and **no** `@import`. Assert **non-exhaustively** — a var from the `colors:` map is present — because the parse is best-effort over reliably-flat top-level maps (`colors:`/`spacing:`); key names vary (`radii:` in the template vs `rounded:` in the dogfood doc) and `typography:` is nested, so the block carries a "refine in DESIGN.md" comment and the assistant completes it *(analyze A2)* (Scenario 2; FR-004).
- [x] T006 [P] **Non-destructive** (C1): with a file beyond `_INDEX.md` under `prototype/`, `init` without `--force` writes nothing and exits 1; `--force` overwrites the starter (Scenario 5; FR-006). Assert the pre-existing user file is preserved on the refuse path.
- [x] T007 [P] **Emergent activation** (C1): after `init`, `prototype/*.html` matches (the `run-qa.sh` tier's own gate) — assert the gate condition, **not** a changed step; `run-qa.sh` is byte-unchanged (Scenario 4; FR-005).
- [x] T008 [P] **Writes only under `prototype/`** (C1): an `init` run leaves `run-qa.sh`, `prototype-sync.md`, `workflow-status.js`, and `.zero-two-one.json` byte-unchanged (FR-005/007).
- [x] T009 [P] **Dispatch route + usage** (C2): `node bin/021.js prototype init` execs `scripts/prototype.js` with `['init']` and returns its exit; `021 prototype` (no leaf) and `021 prototype bogus` ⇒ usage + exit 1 (FR-001). *(analyze-parity with spec 010 A4: keep this in `test/prototype/prototype.test.js`; the `Usage: 021` match in `dispatch.test.js` is additive-safe.)*
- [x] T010 [P] **Cross-stack render** (C3): a `claude` install produces `.claude/commands/021-prototype.md`; an `antigravity` install produces `.agents/skills/021-prototype/SKILL.md` (via the existing `021-*.md` surface-render); **kiro** installs without error and expects no per-command skill (reached via the `021` agent, like `021-feedback`/`021-design`) (Scenario 7; FR-001).

## Phase 3 — Implementation
- [x] T011 `scripts/prototype.js`: the mechanical core — parse `init [--force]` (usage + exit 1 on missing/unknown verb); presence check (any file under `prototype/` except `_INDEX.md`) → refuse + exit 1 without `--force` (write nothing); `mkdir -p prototype/` + seed `_INDEX.md`; resolve `design = loadManifest(root)?.tools.design || 'none'` and `tokensCss = requirements/_design/tokens/*.css` (glob first / conventional `tokens.css`); build the theme reference (`@import` when `design !== none` && tokens exists, else inline `:root` from a minimal, **best-effort** `DESIGN.md` frontmatter read of the reliably-flat maps `colors:`/`spacing:` — **do not hardcode `radii:`** (the dogfood doc uses `rounded:`); skip nested maps like `typography:`; add a "refine in DESIGN.md" comment *(analyze A2)*); write `index.html` (semantic shell + `styles.css`/`app.js` links + a marked screens spot), `styles.css` (theme ref + a few base component styles using `var(--…)`), `app.js` (minimal); print next steps. `fs`/`path` only; **writes only under `prototype/`** (contracts C1; FR-002/003/004/005/006/008).
- [x] T012 `bin/021.js`: add the `prototype` case to `resolve()` — require the `init` leaf (usage on absent/unknown), route to `scripts/prototype.js` with `['init', ...flags]` passed through (incl. `--force`) — and a `prototype init [--force]` line in `USAGE`. No logic in the dispatcher (spec-009 invariant) (C2; FR-001).
- [x] T013 `.claude/commands/021-prototype.md`: the walkthrough (confirm opt-in → `npx 021 prototype init` → build the real screens from the PRD/EDD consuming the design-system vars → note emergent activation + `021-design` re-theme). Uses `npx 021 …` (per spec 009). Auto-renders to the antigravity SKILL surface — no `adapters.js` change. Avoid relative Markdown links (they break at the antigravity/kiro render depth — reference paths as inline code) (C3; FR-001/003). *(analyze-parity A3: add `.claude/commands/021-prototype.md` to `SURFACES.claude` in `dispatch.test.js`.)*
- [x] T014 `scripts/_INDEX.md`: add the `prototype.js` row (mechanical layer; called via `021 prototype init`).

## Phase 4 — Verify & polish
- [x] T015 Run the [quickstart](quickstart.md) end-to-end: scaffold, themed by material-3 vs none, refuse-without-`--force`, run-qa tier flips, optional-never-a-gate, dispatch usage, cross-stack render. Reconcile any drift.
- [x] T016 [P] Wire `test/prototype/prototype.test.js` into `npm test`; full existing suite green; `claude-golden.json` **unchanged** (research R5). Fold the expectation lists: add `021-prototype` to `EXPECTED_COMMANDS` in `test/init/surface.test.js` and confirm the `SURFACES.claude` addition from T013 — both additive, nothing pre-existing breaks.
- [x] T017 [P] `npm run lint` green (`node --check scripts/prototype.js`); **no runtime dependency** (FR-008); `npm run check:links` clean (incl. this spec's `tasks.md`); `npm run sync:package -- --check` clean (the 2 new files land in `package/**`).
- [x] T018 **Dogfood**: regenerate `.zero-two-one.json` (picks up `scripts/prototype.js`, `.claude/commands/021-prototype.md`); run `npx 021 prototype init` in a **scratch copy** to confirm the themed scaffold + the `none` inline `:root`. Do **not** rely on `021 qa` to show the prototype tier here — the dogfood repo is **Phase 1 (mvp)**, whose QA branch does not check the prototype *(analyze A6/A1)*. Verify the repo's own `prototype/` still holds **only** `_INDEX.md` (no stray scaffold committed — the dogfood repo has no prototype).
- [x] T019 Update `specs/_INDEX.md` status; run `npx 021 spec verify 012` clean before `Done`.

## Dependencies (summary)
- Setup: T001 (seams) precedes implementation; T002 (helpers) → all tests.
- Tests (T003–T010) authored before their Phase-3 counterparts; all `[P]`.
- Core order: **T011 (prototype.js) → T012 (route) → T013 (command surface)**; T014 (index) independent, `[P]`.
- Verify (T015–T019) last; T019 gates `Done`.

## Parallelization notes
- Phase-2 tests are independent (`[P]`); T004/T005 both exercise the CSS seam under different `tools.design`; T007/T008 assert the emergent-wire-in invariant (no external file touched).
- T011 is the critical path; T014 can land alongside it. T010's cross-stack assertion needs only the command file (T013), not the script.
