# Tasks: Design-System Install Command (`021-design`)

*Generated from [plan.md](plan.md), [data-model.md](data-model.md), [contracts/design-command.md](contracts/design-command.md), [research.md](research.md), and [quickstart.md](quickstart.md). Dependency-ordered; tests-first. `[P]` = parallelizable. The thin `scripts/design.js` (targeted `tools.design` write + `tokens/` scaffold + marker-bounded `DESIGN.md` section, zero-dep) is the core; the dispatcher route and command surface ride on it. Mirrors spec 010; cross-stack render is free via the spec-007 `021-*.md` surface-render.*

**Proposed layout:** new `scripts/design.js`; new `.claude/commands/021-design.md`; `bin/021.js` (+`design`→`set` route + usage line); `scripts/_INDEX.md` update; tests in `test/design/design.test.js` (new) + two expectation-list folds (`surface.test.js`, `dispatch.test.js`).

## Phase 1 — Setup & guardrails
- [x] T001 **Confirm the seams before writing code.** `scripts/init/manifest.js` exports `loadManifest`/`writeManifest`/`orderFields` → targeted `tools.design` write (NOT `buildManifest`; research R1). The project `DESIGN.md` is **user-owned and may lack a "Design System Mapping" section** (the dogfood one does) → replace-behind-marker with **append-when-absent** (research R2). Marker = `<!-- 021-design:mapping start -->`…`<!-- 021-design:mapping end -->`. `bin/021.js` `resolve()` gets a `design`→`set` leaf (mirrors `spec`). Adding a new command file does **not** re-baseline `claude-golden.json` (research R4). No task changes these facts — this pins them.
- [x] T002 [P] **Test helpers** — a temp-project helper that seeds a git repo + a `.zero-two-one.json` (via `writeManifest`) + a `DESIGN.md` in two shapes (template-with-marker and bespoke-without-marker); a runner for `node scripts/design.js <args>` and `node bin/021.js design <args>` capturing stdout/stderr/exit.

## Phase 2 — Tests first (author before their Phase 3 counterparts)
- [x] T003 [P] **Manifest write** (C1): `set material-3` sets `tools.design: material-3` and refreshes `updatedAt`, while `version`/`phase`/`files`/`merged`/`tools.{stack,assistant,ssd}` are **byte-stable** (no `buildManifest` re-hash) (Scenario 1; FR-007). *(analyze A1: `writeManifest`→`orderFields` is a `FIELD_ORDER` whitelist that DOES include `updatedAt`/`tools`/`files`/`merged`/`hook`/`migrate`, so a targeted write drops nothing — assert this by round-tripping a manifest that carries `hook` + `merged` and confirming both survive intact and `tools` sub-key order is preserved.)*
- [x] T004 [P] **Tokens scaffold** (C1): `requirements/_design/tokens/` is created on first `set` and the command is idempotent on re-run (Scenario 3; FR-005).
- [x] T005 [P] **Mapping section — replace / append / create** (C1): on a `DESIGN.md` that already has the marker, the bounded region is **replaced**; on a bespoke `DESIGN.md` with no marker, the section is **appended** (with markers) and the frontmatter token block is **untouched**; **when `DESIGN.md` is absent, it is created from `templates/DESIGN-Template.md`** (or a minimal doc) first *(analyze A2 — DESIGN.md is user-owned create-if-missing, so a project may have none when `021 design` runs)*; a second identical run is idempotent (Scenario 1/5; FR-004).
- [x] T006 [P] **`none` collapse** (C1): `set none` sets `tools.design: none` and collapses the mapping region to the bespoke note; frontmatter tokens remain (Scenario 6; FR-010).
- [x] T007 [P] **BYO skeleton** (C1): `set <byo-name>` writes generic project-role rows (no `md.sys.*`) and records the system name in `tools.design` (Scenario 2; FR-009).
- [x] T008 [P] **Dispatch route + usage** (C2): `node bin/021.js design set material-3` execs `scripts/design.js` with `['set','material-3']` and returns its exit; `021 design` (no leaf) and `021 design bogus` ⇒ **dispatcher** usage (`Usage: 021`) + exit 1, while `021 design set` (leaf present, system missing) ⇒ routes and **`design.js`** emits its own usage (`Usage: 021 design set`) + exit 1 — assert the two distinct usage sources *(analyze A5)* (FR-001). *(analyze-parity with spec 010 A4: keep this in `test/design/design.test.js`; the `Usage: 021` match in `dispatch.test.js` is additive-safe to the new `design` usage line.)*
- [x] T009 [P] **Cross-stack render** (C3): a `claude` install produces `.claude/commands/021-design.md`; an `antigravity` install produces `.agents/skills/021-design/SKILL.md` (via the existing `021-*.md` surface-render); **kiro** installs without error and expects no per-command skill (reached via the `021` agent, like `021-feedback`/`021-init`) (Scenario 8; FR-001).

## Phase 3 — Implementation
- [x] T010 `scripts/design.js`: the mechanical core — parse `set <system>` (usage + exit 1 if the verb or system is missing); `loadManifest(root)` → set `tools.design` + refresh `updatedAt` → `writeManifest` (targeted, no rebuild); `mkdir -p requirements/_design/tokens/` and drop a **link-free** keep file (`.gitkeep` or a link-free `_INDEX.md`) so the dir is tracked and `check-links` stays clean *(analyze A3)*; edit `DESIGN.md` — **create it from `templates/DESIGN-Template.md` if absent** *(analyze A2)*, then locate the marker region and **replace**, else **append** the section (+ markers) after the body, leaving the frontmatter block untouched; emit the system skeleton (`material-3` md.sys.* rows / `<byo>` generic rows / `none` collapse); print next steps (assess, fill values, import export, annotate EDD, re-theme prototype if present, record via a refinement round). `fs`/`path` only (contracts C1; FR-002/004/005/007/010/011/012).
- [x] T011 `bin/021.js`: add the `design` case to `resolve()` — require the `set` leaf (usage on absent/unknown), route to `scripts/design.js` with `['set', <system>]` passed through — and a `design set <system>` line in `USAGE`. No logic in the dispatcher (spec-009 invariant) (C2; FR-001).
- [x] T012 `.claude/commands/021-design.md`: the DSS walkthrough (select → assess → `npx 021 design set <system>` → map values + import export → cascade CSS-vars/prototype + EDD annotation → record via a refinement round). Uses `npx 021 …` (per spec 009). Auto-renders to the antigravity SKILL surface — no `adapters.js` change (C3; FR-001/003/006/011). *(analyze-parity A3: add `.claude/commands/021-design.md` to `SURFACES.claude` in `dispatch.test.js` T006.)*
- [x] T013 `scripts/_INDEX.md`: add the `design.js` row (mechanical layer; called via `021 design set`).

## Phase 4 — Verify & polish
- [x] T014 Run the [quickstart](quickstart.md) end-to-end: adopt material-3, BYO, switch, remove→none, prototype no-op, dispatch usage, cross-stack render. Reconcile any drift.
- [x] T015 [P] Wire `test/design/design.test.js` into `npm test`; full existing suite green; `claude-golden.json` **unchanged** (research R4). Fold the expectation lists: add `021-design` to `EXPECTED_COMMANDS` in `test/init/surface.test.js` and confirm the `SURFACES.claude` addition from T012 — both additive, nothing pre-existing breaks.
- [x] T016 [P] `npm run lint` green (`node --check scripts/design.js`); **no runtime dependency** (FR-012); `npm run check:links` clean (incl. this spec's `tasks.md`); `npm run sync:package -- --check` clean (the 2 new files land in `package/**`).
- [x] T017 **Dogfood**: regenerate `.zero-two-one.json` (picks up `scripts/design.js`, `.claude/commands/021-design.md`); run `npx 021 design set material-3` on a scratch copy of the repo's `DESIGN.md` to confirm the section is appended (the dogfood doc has no marker) and `tools.design` flips — then **fully restore** the repo's own `design: none` / bespoke `DESIGN.md` **and remove the `requirements/_design/tokens/` dir the dogfood run created** *(analyze A3)* so the dogfood repo stays on `none` with no stray artifacts.
- [x] T018 Update `specs/_INDEX.md` status; run `npx 021 spec verify 011` clean before `Done`.

## Dependencies (summary)
- Setup: T001 (seams) precedes implementation; T002 (helpers incl. the two `DESIGN.md` shapes) → all tests.
- Tests (T003–T009) authored before their Phase-3 counterparts; all `[P]`.
- Core order: **T010 (design.js) → T011 (route) → T012 (command surface)**; T013 (index) independent, `[P]`.
- Verify (T014–T018) last; T018 gates `Done`.

## Parallelization notes
- Phase-2 tests are independent (`[P]`); T005 (replace-vs-append) leans on both `DESIGN.md` shapes from T002.
- T010 is the critical path; T013 can land alongside it. T009's cross-stack assertion needs only the command file (T012), not the script.
